type Auth {
    access_token: String
    refresh_token: String
    token_type: String
    expires_in: Int
}

extend type Mutation {
    login(email: String!, password: String!): Auth!
    @field(resolver: "LoginMutator@login")

    refresh: Auth!
    @field(resolver: "LoginMutator@refresh")
}
