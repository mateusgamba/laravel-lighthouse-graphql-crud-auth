type Auth {
    token: String
    token_refresh: String
    token_type: String
    expires_in: Int
}

extend type Mutation {
    login(email: String!, password: String!): Auth!
    @field(resolver: "LoginMutator@login")
}
